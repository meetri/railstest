<p id="notice"><%= notice %></p>
<h1>Shared This Badge with <%=@user.email%></h1>
<p>
  <b>Name:</b>
  <%= @badge.name %>
</p>

<% unless @badge.image.nil? %>
  <p>
    <b>Image:</b>
    <%=@badge.show_image("320x250#").html_safe unless @badge.image.nil? %>    
  </p>
<% end %>




<%= link_to 'Edit', edit_badge_path(@badge) %> |
<%= link_to 'Back', badges_path %>
