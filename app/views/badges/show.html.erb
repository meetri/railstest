<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @badge.name %>
</p>

<% unless @badge.image.nil? %>
<p>
  <b>Image:</b>
  <img src="<%= @badge.image.url("300x250#") %>" />
</p>
<% end %>




<%= link_to 'Edit', edit_badge_path(@badge) %> |
<%= link_to 'Back', badges_path %>
